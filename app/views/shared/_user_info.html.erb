<a href="<%= user_path(current_user)%>">
  <%= gravater_for current_user, size: 52 %>
</a>
<h1><%= current_user.name %></h1>
<span>
  <%= link_to "view my profie", current_user %>
</span>
<span>
  <%= pluralize(current_user.microposts.count, "micropost") %>
</span>
